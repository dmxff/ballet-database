{
  "name": "hamburg-ballet-database",
  "version": "1.0.0",
  "description": "Automatically builds and updates a database of upcoming Hamburg Ballett performances by scraping the official Hamburg Ballett website.",
  "private": true,
  "main": "scripts/scrape.js",
  "scripts": {
    "fetch": "node scripts/scrape.js",
    "fetch:hamburg": "node scripts/scrape.js",
    "fetch:berlin": "node scripts/scrape-berlin.js",
    "fetch:stuttgart": "node scripts/scrape-stuttgart.js",
    "fetch:all": "npm run fetch:hamburg && npm run fetch:berlin && npm run fetch:stuttgart",
    "setup:playwright": "node ./node_modules/playwright/cli.js install chromium",
    "postinstall": "node ./node_modules/playwright/cli.js install chromium"
  },
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.47.0"
  },
  "engines": {
    "node": ">=18"
  }
}
