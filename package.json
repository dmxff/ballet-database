{
  "name": "hamburg-ballet-database",
  "version": "1.0.0",
  "description": "Automatically builds and updates a database of upcoming Hamburg Ballett performances by scraping the official Hamburg Ballett website.",
  "private": true,
  "main": "src/scrape.js",
  "scripts": {
    "fetch": "node src/scrape.js",
    "postinstall": "playwright install --with-deps chromium"
  },
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.47.0"
  },
  "engines": {
    "node": ">=18"
  }
}
